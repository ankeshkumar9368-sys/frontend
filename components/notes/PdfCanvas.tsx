import React from 'react';

const PdfCanvas = (props: any) => {
  return (
    <div className="flex-1 flex items-center justify-center bg-gray-100">
      <p>PdfCanvas component (Stub)</p>
    </div>
  );
};

export default PdfCanvas;
